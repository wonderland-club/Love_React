import { ROOT_COMPONENT_NAME } from "../../route-constants";

const RootComponent = () => {
  return (
    <div>
      {`${ROOT_COMPONENT_NAME} Page`}
    </div>
  );
};

export default RootComponent;
